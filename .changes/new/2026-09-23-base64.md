NEW: base64 — 15-entry public API (encode/decode/encode_into/decode_into/encoded_len/decoded_len/is_valid/Encoder/Decoder + Alphabet/Padding/PaddingMode/Whitespace/ErrorKind/Base64Error), covering base64, base64url, base32, base32hex and base16.
FIX: base64 — streaming decoder now accepts a padding run that crosses a chunk boundary; only end of stream rejects an incomplete run (INVALID_PADDING).
INTERNAL: new library layout and workflow rework (one file per API entry, inline API docs, _internal/ for shared code, _dev/ for research and design record).
INTERNAL: root task test / task ci auto-discover every library; .changes/ drives the changelog and the 0.x.y tag.
